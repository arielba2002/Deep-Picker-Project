/* src/components/Lineup.css */

.lineup-container {
  width: 100%;
  height: 70%; /* Adjust height as needed */
  background-image: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcS6mh-qVpI8BiWhC_SeihtBj99in2Jb5JiM5A&s'); /* Ensure the image is in the public folder */
  background-size: cover;
  background-position: center;
  position: relative;
  border: 2px solid #ccc;
}

/* Style for each player (displayed as a circle with a label) */
.player {
  position: absolute;
  width: 40px;
  height: 40px;
  background-color: #ff5722; /* Change color if desired */
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: bold;
  transform: translate(-50%, -50%);
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
}

